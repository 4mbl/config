{
  "$schema": "./node_modules/turbo/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@4mbl/tsconfig#build": {
      "dependsOn": ["^build"],
      "outputs": ["dist"]
    }
  }
}
